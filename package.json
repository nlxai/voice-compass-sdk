{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.6"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna run build",
    "postinstall": "npm run bootstrap"
  }
}
